# =============================================================================
# Project O.R.B.I.T. — environment template
# Copy to .env and fill in real values. NEVER commit .env itself.
# =============================================================================

# --- Google Cloud -------------------------------------------------------------
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# --- API security ---------------------------------------------------------------
# Generate a strong key with:  openssl rand -hex 32
# If unset, the API starts WITH authentication disabled (local-dev convenience).
ORBIT_API_KEY=

# --- Model overrides (optional; defaults shown) -----------------------------------
ORBIT_COMMANDER_MODEL_ID=gemini-2.5-pro
ORBIT_ASTRO_MODEL_ID=gemini-2.5-flash
ORBIT_DIPLOMAT_MODEL_ID=gemini-2.5-flash
ORBIT_VERTEX_MODEL_ID=gemini-3.5-flash
# Bonus integrations (Phase 7):
ORBIT_EDGE_MODEL_ID=gemma-3-27b-it        # satellite-side autopilot (Vertex AI Gemma)
ORBIT_VEO_MODEL_ID=veo-3.0-generate-001   # mission-debrief video
ORBIT_LYRIA_MODEL_ID=lyria-002            # mission-control audio cues

# --- Phase 7 switches --------------------------------------------------------------
# Onboard autonomy fallback (negotiation/armour breaker trips on HIGH risk).
ORBIT_ENABLE_EDGE_AUTONOMY=true
# Real Vertex AI generation for Veo debriefs / Lyria cues. Unset = clearly-
# labelled offline simulations (SVG reconstruction / procedural synth).
# ORBIT_ENABLE_REAL_VEO=1
# ORBIT_ENABLE_REAL_LYRIA=1

# --- Persistence ------------------------------------------------------------------
# auto = Firestore when credentials/emulator/project are present, else in-memory.
ORBIT_MEMORY_BACKEND=auto
# For local Firestore emulation:
# FIRESTORE_EMULATOR_HOST=localhost:8080

# --- Phase 8: long-running watches ---------------------------------------------------
ORBIT_WATCH_DEFAULT_INTERVAL_HOURS=6
ORBIT_WATCH_ESCALATE_BAND=HIGH
ORBIT_WATCH_SUPERVISOR_POLL_SECONDS=60

# --- Phase 8: real Space-Track.org data -----------------------------------------------
# Leave unset to run on the calibrated synthetic catalogue (logged fallback).
# SPACETRACK_USERNAME=
# SPACETRACK_PASSWORD=
SPACETRACK_CACHE_TTL_SECONDS=21600
SPACETRACK_MIN_INTERVAL_SECONDS=3

# --- Phase 8: vector memory -------------------------------------------------------------
ORBIT_EMBED_MODEL_ID=text-embedding-005

# --- Cloud Run injects PORT automatically (default 8080 locally) --------------------
PORT=8080
